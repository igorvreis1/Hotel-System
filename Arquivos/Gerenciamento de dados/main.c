#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "dados.h"

 int main(void)//editar hotel
{
    tipoHotel hotel;
    memset(&hotel, 'a', sizeof(hotel));//setar todo esse pedaço de memoria com o caracter de delete

  return 0;
}